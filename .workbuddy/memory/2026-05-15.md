# 2026-05-15 工作日志

## 仓库与背包配置 UI 设计（7.2 节完整设计）—— 已于 2026-05-15 重写

重写内容详见 `client/doc/完整游戏策划方案.md` 7.1 和 7.2 节（本次大改）

### 大厅（7.1 节）新增内容
- 快捷入口新增核心按钮「**战前战备**」（金色实心，大厅右栏最显著按钮）
  - 进入 backpack_config 场景（战前战备联合场景）
- ~~保留「物资仓库」按钮~~ → **已删除**：大厅不再有独立的「物资仓库」入口，该按钮为冗余设计

### 战前战备（7.2 节重写）—— 原"仓库与背包配置"
- **场景定性变化**：从弹出层改为全屏独立场景（scene 切换，非弹窗）
- **核心设计**：左右并排同时显示仓库（左 48%）和背包（右 48%），物品可双向自由拖拽
- **售卖/分解按钮场景边界（关键规则）**：
  - ✅ 仅在战前战备场景（backpack_config）的仓库区 和 独立仓库场景（warehouse）中出现
  - ✗ 游戏中背包（game_stage 内 Backpack.tscn）严禁出现售卖按钮
  - ✗ 宝箱开启界面严禁出现售卖按钮
  - ✗ 结算界面严禁出现售卖按钮
- **仓库区卡片新增**：在卡片底部固定显示「装入背包」+「售出」两个小按钮（无需长按）
- **背包区物品操作**：点击已放置物品弹出气泡，仅有「取出物品」+「查看详情」，**无售出**
- **拖拽规则**：六种操作（仓库→背包、背包→仓库、背包内移动、旋转、取消、快捷放置）
- **代码对接**：`UIManager.change_scene("backpack_config")` 进入该场景

## 网络交互架构设计文档
- 创建了 `网络交互架构设计.md` 在项目主目录
- 分析了当前前后端实现现状：
  - 后端 Go：已有 WebSocket 服务、房间管理、权威游戏循环（20Hz）
  - 前端 Godot：缺少 NetworkManager.gd（核心待办），房间场景已引用但不存在
- 设计了完整的消息协议规范（JSON 格式）
  - 客户端→服务端：JOIN, LEAVE_ROOM, READY_TOGGLE, START_GAME, MOVE, BOMB 等
  - 服务端→客户端：WELCOME, ROOM_STATE, GAME_START, PLAYER_MOVE, BOMB_EXPLODED 等
- 设计了大厅 HTTP API（当前缺失）：
  - POST /create_room → 返回 room_id + ws_url + token
  - POST /join_room → 返回 ws_url + token
  - GET /room_list → 返回房间列表
- 设计了重连机制（Token 验证 + 状态恢复）
- 提供了分步实现指南（6 步）
