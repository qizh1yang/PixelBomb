# 2026-05-07

## 十字形5区域地图重构
- 从H形地图完全重写为十字形5区域地图，尺寸从 17×15 改为 25×25
- 5个区域：四角房间(各8×8) + 中心十字通道
- 每个区域内部都是经典炸弹人布局（偶数坐标柱子 + 可破坏墙）
- 区域间不可破坏分隔墙（x=9,15 和 y=9,15），每面开2格固定缺口
- 修改文件：`wall_layer.gd`（完全重写）、`floor_layer.gd`（尺寸改25×25）
- `game_mode.gd` 无需修改（出生点用 `get_used_rect()` 动态计算）
