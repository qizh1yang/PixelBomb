# PixelBomb 代码开发规范

## 1. 终端指令规范
- 在 Windows 环境下，所有的终端指令必须使用 **PowerShell** 原生命令（例如使用 `Get-ChildItem` 代替 `dir`，使用 `Select-String` 代替 `grep` 等）。
- 禁止使用 CMD 格式的指令，以确保在当前开发环境下的一致性和稳定性。

## 2. 操作流程规范
- **每日首要任务**：在每一天进行第一次代码操作（包括查看、修改、运行指令等）之前，必须首先查看一遍本代码规范文件（`CODE_SPEC.md`）。
- **执行确认**：在确认已阅读并理解规范后，方可继续后续的开发任务。
